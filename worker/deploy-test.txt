ทดสอบ Cloudflare Worker Auto Deploy

ไฟล์นี้เอาไว้ใส่ในโฟลเดอร์ worker/ เพื่อ trigger build ของ Cloudflare Worker ผ่าน GitHub

ตำแหน่งที่ต้องวาง:
worker/deploy-test.txt

ผลที่ต้องเห็น:
Cloudflare > Worker white-rice-cf72 > Deployments / Build history ขึ้น build ใหม่ และจบด้วย Success

ไฟล์นี้ไม่กระทบ worker.js เพราะเป็นไฟล์ txt ธรรมดา ใช้สำหรับทดสอบระบบ auto deploy เท่านั้น

Prepared: 2026-05-03
